// Public surface of the "home" feature.
// Each feature folder is self-contained: components/, hooks/, services/,
// types/ as needed, exposing only what other parts of the app should use.
export {}
